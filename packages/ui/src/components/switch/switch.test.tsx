import { render, screen } from "@testing-library/react";
import React from "react";
import { Switch } from "./switch";

describe("Switch", () => {
  it("should...", () => {
    render(<Switch />);
  });
});
